import { ProficiencyPipe } from './proficiency.pipe';

describe('ProficiencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ProficiencyPipe();
    expect(pipe).toBeTruthy();
  });
});
